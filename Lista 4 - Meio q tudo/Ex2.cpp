    #include <iostream>

    using namespace std;

    int main(){
        float a,b;
        cin>>a>>b;
        if (a>b) cout<<"Primeiro eh maior.";
        else if (a<b) cout<<"Segundo eh maior.";
        else if (a==b) cout<<"Numeros iguais.";
    }